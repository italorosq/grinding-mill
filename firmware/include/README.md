# Include

Pasta reservada para headers (`.h`) e interfaces do firmware.

## Sugestao de crescimento

- `pins.h`: mapeamento centralizado de pinos.
- `menu.h/.cpp`: separacao da logica de interface.
- `motor.h/.cpp`: encapsulamento do controle do motor.
